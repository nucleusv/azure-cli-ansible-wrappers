

 - name: Set input params for az_webapp_config_connection_string_list
   set_fact: 
     az_webapp_config_connection_string_list:
       input: 
         ###  Required parameter. Name of the web. You can configure the default using 'az configure --defaults web=<name>'.  ### 
        'name': "{{ item['name'] | default('') }}" 
         ###  Required parameter. Name of resource group. You can configure the default group using `az configure --defaults group=&lt;name&gt;`.  ### 
        'resource-group': "{{ item['resource-group'] | default('') }}" 
         ###  Optional parameter. The name of the slot. Default to the productions slot if not specified.  ### 
        'slot': "{{ item['slot'] | default('') }}" 
 - debug: msg="{{ az_webapp_config_connection_string_list }}"          
 - name: "az webapp config connection-string list: " 
   command: |                                      
     az webapp config connection-string list                            
                 --name {{ az_webapp_config_connection_string_list.input['name'] }} 
                 --resource-group {{ az_webapp_config_connection_string_list.input['resource-group'] }} 
                 {%if az_webapp_config_connection_string_list.input['slot'] %} --slot az_webapp_config_connection_string_list.input['slot'] {%endif %} 
   register: output  
 - set_fact: 
    az_webapp_config_connection_string_list: "{{ az_webapp_config_connection_string_list|combine({'output':output.stdout|from_json}) }}"  
 - debug: msg="{{ az_webapp_config_connection_string_list }}" 
